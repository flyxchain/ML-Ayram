# requirements.txt — Dependencias locales (Windows, Python 3.14)
# Instalar con: pip install -r requirements.txt
#
# ─── NOTA Python 3.14 ───────────────────────────────────────────────────────
# TensorFlow no tiene wheel para Python 3.14 — se usa PyTorch para la LSTM.
# TA-Lib requiere instalación manual del .whl:
#   Descarga: https://github.com/cgohlke/talib-build/releases
#   Instala:  pip install TA_Lib-0.x.x-cpXX-cpXX-win_amd64.whl
# mlflow no compila en Python 3.14 (pyarrow requiere cmake+MSVC) — es opcional.
# ────────────────────────────────────────────────────────────────────────────

# ── Datos ────────────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=2.0.0
ta>=0.11.0              # indicadores técnicos (importado en features.py)

# ── Machine Learning ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0
torch>=2.2.0            # PyTorch para LSTM
optuna>=3.4.0           # búsqueda de hiperparámetros
joblib>=1.3.0           # serialización de modelos (sklearn/xgboost)

# ── Base de datos ────────────────────────────────────────────────────────────
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# ── Dashboard ────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0         # modelos de validación en app.py

# ── HTTP ─────────────────────────────────────────────────────────────────────
requests>=2.31.0        # collector.py (EODHD API) y telegram.py

# ── Utilidades ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=4.1.0       # cobertura de tests
httpx>=0.26.0           # requerido por FastAPI TestClient
pytest-mock>=3.12.0     # fixtures de mocking

# ── Desarrollo y análisis (solo local, no se instalan en servidor) ────────────
jupyter>=1.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
